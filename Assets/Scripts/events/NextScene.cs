using UnityEngine;

namespace Events
{
    public class NextScene : MonoBehaviour
    {
        /**
    private void OnTriggerEnter(Collider other)
    {
        if (other.CompareTag("Ratte") && false)
        {
            SceneManager.LoadScene("Raum_02V2",LoadSceneMode.Single);
            
        }else
        {
            Debug.Log("..wenn du eine Ratte wärst");
        }


    }
**/
        // Start is called before the first frame update
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {

        }
    }
}